@model MVCproyect.Models.Product

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Add Product</title>
</head>
<body>

    <div class="container mt-5">

        <!-- Encabezado del formulario -->
        <header class="text-center mb-4">
            <h1 class="display-4 text-primary">Add Product</h1>
            <p class="text-muted">Fill in the details below to add a new product.</p>
        </header>

        <!-- Formulario con ancho ajustado -->
        <form asp-action="Create" method="post" class="p-4 rounded shadow-lg bg-white border" style="max-width: 500px; margin: 0 auto;">

            <div class="mb-4">
                <label asp-for="Name" class="form-label font-weight-bold">Product Name</label>
                <div class="input-group">
                    <span class="input-group-text bg-primary text-white">
                        <i class="fas fa-box"></i>
                    </span>
                    <input type="text" asp-for="Name" class="form-control" placeholder="Enter product name" />
                </div>
                <span asp-validation-for="Name" class="text-danger"></span>
            </div>

            <div class="mb-4">
                <label asp-for="Description" class="form-label font-weight-bold">Description</label>
                <div class="input-group">
                    <span class="input-group-text bg-primary text-white">
                        <i class="fas fa-pencil-alt"></i>
                    </span>
                    <textarea asp-for="Description" class="form-control" placeholder="Enter product description"></textarea>
                </div>
                <span asp-validation-for="Description" class="text-danger"></span>
            </div>

            <div class="mb-4">
                <label asp-for="Stock" class="form-label font-weight-bold">Stock</label>
                <div class="input-group">
                    <span class="input-group-text bg-primary text-white">
                        <i class="fas fa-cogs"></i>
                    </span>
                    <input type="number" asp-for="Stock" class="form-control" placeholder="Enter stock quantity" />
                </div>
                <span asp-validation-for="Stock" class="text-danger"></span>
            </div>

            <div class="mb-4">
                <label asp-for="Price" class="form-label font-weight-bold">Price</label>
                <div class="input-group">
                    <span class="input-group-text bg-primary text-white">
                        <i class="fas fa-dollar-sign"></i>
                    </span>
                    <input type="number" asp-for="Price" class="form-control" placeholder="Enter product price" />
                </div>
                <span asp-validation-for="Price" class="text-danger"></span>
            </div>

            <!-- Botón de crear producto -->
            <div class="d-grid gap-2 d-md-flex justify-content-md-end mt-5">
                <button type="submit" class="btn btn-primary btn-lg">
                    <i class="fas fa-plus"></i> Create Product
                </button>
                <a href="@Url.Action("Index")" class="btn btn-secondary btn-lg">
                    <i class="fas fa-arrow-left"></i> Back to List
                </a>
            </div>
        </form>
    </div>

</body>
</html>
